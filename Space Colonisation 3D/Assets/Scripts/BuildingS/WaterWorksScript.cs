using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WaterWorks : BuildingScript
{

    public WaterWorks() 
    {
        Name = "waterworks";
        oreCost = 200;
        energyCost = 50;
        maxManpower = 10;
    }

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
